// Copy this file to Signing.xcconfig (gitignored) and set your Apple Developer
// Team ID. This keeps the Team ID out of the public repo while letting signing
// survive `xcodegen generate` (project.yml references it via configFiles).
//
// Find your Team ID at https://developer.apple.com/account (Membership Details),
// or from an installed signing certificate:
//   security find-certificate -c "Apple Development" -p | openssl x509 -noout -subject
// (the OU=... field is your Team ID).
DEVELOPMENT_TEAM = YOUR_TEAM_ID

// Production config baked into the Info.plist (WorkerBaseURL / RevenueCatKey) so a
// device / TestFlight build reaches the Worker + configures RevenueCat without the
// DEBUG READER_* env vars. Host only — no scheme (xcconfig treats `//` as a comment,
// so the https:// is added in project.yml). REVENUECAT_KEY is the SDK public key
// (appl_/test_). Leave blank → the app falls back to its placeholder.
WORKER_HOST = your-worker.example.workers.dev
REVENUECAT_KEY =
