# Yomi — local secret for the alignment-capture script. Copy to `.env` (gitignored).
#
# This file is read ONLY by scripts/capture-alignment.mjs (Node). The iOS app reads
# its config (Worker host, RevenueCat key) from the gitignored Signing.xcconfig →
# Info.plist — NOT from here. There are no DEBUG launch-env overrides anymore.

# ElevenLabs API key — used ONLY by scripts/capture-alignment.mjs. Never shipped.
ELEVEN_KEY=
